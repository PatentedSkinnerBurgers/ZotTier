export type TierList = {
  id: number;
  imageUrl: string;
  itemCount: number;
  name: string;
  totalVotes: number;
};

export type TierListItem = {
  index: number;
  name: string;
  imageUrl: string;
};
